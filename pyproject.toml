[project]
name = "baby-agent"
version = "0.1.0"
description = "A smart command-line agent with OpenAI integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0"
]

[project.scripts]
baby-agent = "baby_agent.cli:main"

